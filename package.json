{
  "name": "focus-onion",
  "version": "1.0.0",
  "description": "Cloudflare Worker Project",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate:remote": "wrangler d1 migrations apply focus-onion-db --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260304.0",
    "drizzle-kit": "^0.31.9",
    "typescript": "^5.9.3",
    "wrangler": "^4.68.1"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  }
}
